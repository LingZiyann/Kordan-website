import classes from "./Section2.module.css";

const Section2 = () => {
    return(
        <section className={classes.Section2Container}>
            <div class={classes.popupContainer}>
                <h1 class={classes.popup}>"Mid Gap"</h1>
                <h2>Kordan is a world class Mid player who has dominated big names like Topson, TorontoTokyo, Armel, Karl, etc</h2>
            </div>
            <div>

            </div>
        </section>
    );
};

export default Section2;